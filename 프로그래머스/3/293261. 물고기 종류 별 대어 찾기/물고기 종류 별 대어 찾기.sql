SELECT A.ID, (SELECT FISH_NAME FROM FISH_NAME_INFO WHERE FISH_TYPE=B.FISH_TYPE) AS FISH_NAME, A.LENGTH FROM FISH_INFO AS A JOIN (SELECT FISH_TYPE, MAX(LENGTH) AS LENGTH FROM FISH_INFO GROUP BY FISH_TYPE) AS B ON (A.FISH_TYPE=B.FISH_TYPE and A.LENGTH=B.LENGTH) ORDER BY A.ID
